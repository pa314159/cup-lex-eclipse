<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>net.sf</groupId>
		<artifactId>cup-lex-eclipse</artifactId>
		<version>1.0.0-SNAPSHOT</version>
	</parent>

	<groupId>net.sf.cup-lex-eclipse</groupId>
	<artifactId>pi.eclipse.cle.feature</artifactId>

	<description>CLE Eclipse Feature</description>

	<build>
		<resources>
			<resource>
				<directory>.</directory>
				<includes>
					<include>feature.*</include>
				</includes>
				<filtering>true</filtering>
			</resource>
		</resources>
		<plugins>
			<plugin>
				<groupId>${project.groupId}</groupId>
				<artifactId>cup-lex-maven-plugin</artifactId>
				<version>${project.version}</version>
				<executions>
					<execution>
						<goals>
							<goal>update-version</goal>
						</goals>
						<configuration>
							<includes>
								<include>**/feature.*</include>
							</includes>
							<replace>0.1.2.3-VERSIONSUFFIX</replace>
							<with>${project.version}-${version-suffix}</with>
							<property>plugin-version</property>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<artifactId>maven-dependency-plugin</artifactId>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>copy</goal>
						</goals>
						<configuration>
							<outputDirectory>${project.build.directory}/plugins</outputDirectory>
							<artifactItems>
								<artifactItem>
									<groupId>${project.groupId}</groupId>
									<artifactId>pi.eclipse.cle</artifactId>
									<destFileName>pi.eclipse.cle_${plugin-version}.jar</destFileName>
								</artifactItem>
							</artifactItems>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<artifactId>maven-assembly-plugin</artifactId>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
							<goal>directory-single</goal>
						</goals>
						<configuration>
							<descriptors>
								<descriptor>eclipse.xml</descriptor>
							</descriptors>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	<dependencies>
		<dependency>
			<groupId>${project.groupId}</groupId>
			<artifactId>pi.eclipse.cle</artifactId>
			<version>${project.version}</version>
		</dependency>
	</dependencies>
</project>
